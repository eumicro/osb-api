import type { OsbPlugin } from "./types";

/** Example plugin registration for the catalog feature area. */
export const catalogPlugin: OsbPlugin = {
  id: "catalog",
  navLabelKey: "nav.catalog",
};
